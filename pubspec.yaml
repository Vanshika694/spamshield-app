name: spamshield
description: "SpamShield - SMS Spam Detection App"
publish_to: 'none'
version: 1.0.0+3

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  fl_chart: ^0.70.2
  google_fonts: ^6.2.1
  shared_preferences: ^2.3.2
  animate_do: ^3.3.4
  http: ^1.2.1
  permission_handler: ^11.3.1
  flutter_sms_inbox: ^1.0.4
  google_sign_in: ^6.2.1
  flutter_onnxruntime: ^1.6.0
  flutter_embedder: ^0.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model/tokenizer.json
    - assets/model/model.onnx